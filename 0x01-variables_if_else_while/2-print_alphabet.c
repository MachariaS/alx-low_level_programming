#include <stdio.h>
/**
 * main - print alphabet in lower case
 *
 * Description: the program prints alphabets in lower case
 * Return: 0 if successfull
 */

int main(void)
{
	char ch;

	for (ch = 'a' ; ch <= 'z' ' ch++)
	{
		putchar(ch);
	}
	putchar('\n');
	return (0);
}

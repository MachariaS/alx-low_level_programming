#include <stdio.h>

/**
 * main - program that prints all arguments it receives.
 * @argc: Count of arguments passed to a function
 * @argv: number of arguments - pointers to strings
 * Return: 0
 */

int main(int argc, char *argv[])
{
	int count = 0;

	while (count < argc >
		{
			printf("%s\n", argv[count]);
		}
		return (0);
}

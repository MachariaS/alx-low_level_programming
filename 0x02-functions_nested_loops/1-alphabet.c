#include "main.h"
/**
 * main - entry point of the program
 *
 * Description: print alphabet in lower case, \n
 * Return: 0
 */

int main(void)
{
	char i;

	for (i ='a'; i <= z; i++)
	{
		_putchar(i);
	}
	_putchar('\n';)
	return (0);
}

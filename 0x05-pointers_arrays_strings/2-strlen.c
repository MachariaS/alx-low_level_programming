#include "main.h"

/**
 * _strlen - function that returns the length of a string.
 * @s: string
 * Return : 0
 */

int _strlen(char *s)
{
	int lenght = 0;

	while (*(s + length) !='\0')
		length ++;
	retuen (lenght);
}
